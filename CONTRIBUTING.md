Thank you for considering contributing to the RSS AdRock Generator project! 🙌

_Este documento está em português. Scroll down for English version._  
_This document is in Portuguese. Scroll down for English version._

# Contribuindo com o projeto RSS AdRock Generator

Obrigado por seu interesse em contribuir com este projeto! 🎉

## Como contribuir

1. Fork este repositório
2. Crie um branch: `git checkout -b minha-contribuicao`
3. Faça suas alterações
4. Commit e push: `git commit -m "minha contribuição"` e `git push origin minha-contribuicao`
5. Abra um Pull Request com uma descrição clara das mudanças

## Boas práticas

- Use `Python 3.9+`
- Teste localmente com `venv`
- Mantenha a estrutura dos arquivos clara e documentada

---

# Contributing to RSS AdRock Generator

Thank you for your interest in contributing to this project! 🎉

## How to contribute

1. Fork this repository  
2. Create a branch: `git checkout -b my-contribution`  
3. Make your changes  
4. Commit and push: `git commit -m "my contribution"` and `git push origin my-contribution`  
5. Open a Pull Request with a clear description of the changes

## Best practices

- Use `Python 3.9+`  
- Test locally using `venv`  
- Keep file structure clear and well-documented

Thank you for considering contributing to the RSS AdRock Generator project! 🙌

_Este documento está em português. Scroll down for English version._  
_This document is in Portuguese. Scroll down for English version._

# Contribuindo com o projeto RSS AdRock Generator

Obrigado por seu interesse em contribuir com este projeto! 🎉  
Este projeto segue uma abordagem técnica, simples e objetiva. Contribuições são bem-vindas desde que mantenham a clareza e a estabilidade do pipeline.

---

## 🚀 Como contribuir

1. Fork este repositório
2. Crie um branch descritivo:
   ```
   git checkout -b feat/nome-da-feature
   ```
   ou
   ```
   git checkout -b fix/nome-do-ajuste
   ```
3. Faça suas alterações
4. Commit seguindo padrão claro:
   ```
   git commit -m "feat: descrição objetiva da mudança"
   ```
5. Envie para seu fork:
   ```
   git push origin nome-do-branch
   ```
6. Abra um Pull Request com:
   - Descrição clara da alteração
   - Justificativa técnica
   - Impacto esperado

---

## 🧠 Boas práticas técnicas

- Use **Python 3.9+**
- Teste localmente com `venv`
- Não inclua:
  - `venv/`
  - `logs.db`
  - Arquivos temporários
- Mantenha dependências mínimas
- Preserve compatibilidade com Linux (Ubuntu 22.04+)
- Evite alterar a estrutura do RSS sem necessidade técnica

---

## 🔐 Segurança

- Não exponha chaves
- Não inclua dados sensíveis
- Consulte `SECURITY.md` para reporte responsável

---

## 📝 Padrão de commits

Utilize convenção simples baseada em prefixos:

- `feat:` nova funcionalidade
- `fix:` correção
- `refactor:` melhoria estrutural
- `docs:` documentação
- `chore:` ajustes internos

---

# Contributing to RSS AdRock Generator

Thank you for your interest in contributing! 🎉  
This project follows a technical, clean, and minimalistic architecture. Contributions are welcome as long as they preserve clarity and pipeline stability.

---

## 🚀 How to contribute

1. Fork this repository
2. Create a descriptive branch:
   ```
   git checkout -b feat/feature-name
   ```
   or
   ```
   git checkout -b fix/bug-name
   ```
3. Make your changes
4. Commit using a clear convention:
   ```
   git commit -m "feat: concise description of change"
   ```
5. Push to your fork:
   ```
   git push origin branch-name
   ```
6. Open a Pull Request including:
   - Clear description
   - Technical rationale
   - Expected impact

---

## 🧠 Technical best practices

- Use **Python 3.9+**
- Test locally with `venv`
- Do NOT commit:
  - `venv/`
  - `logs.db`
  - Temporary files
- Keep dependencies minimal
- Maintain Linux compatibility (Ubuntu 22.04+)
- Avoid modifying RSS structure without technical necessity

---

## 🔐 Security

- Do not expose keys
- Do not commit sensitive data
- See `SECURITY.md` for responsible disclosure guidelines

---

## 📝 Commit convention

Use a simple prefix-based convention:

- `feat:` new feature
- `fix:` bug fix
- `refactor:` structural improvement
- `docs:` documentation
- `chore:` internal adjustment